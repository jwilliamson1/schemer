#lang racket
(define f
  (let ((state null))
    (define (dispatch v)
      (cond ((eq? state null)(begin (set! state v) state))
            (else 0)))
    dispatch))


(+ (f 0) (f 1))

(+ (f 1) (f 0))